# Default values for python-app.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: devops-python-app
  tag: v1
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80

env:
  APP_ENV: dev
