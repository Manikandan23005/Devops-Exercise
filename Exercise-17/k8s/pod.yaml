apiVersion: v1
kind: Pod
metadata:
  name: dynamodb-test
  namespace: exercise17
spec:
  serviceAccountName: dynamodb-sa
  containers:
  - name: aws-cli
    image: amazon/aws-cli
    command:
      - sleep
      - "3600"
