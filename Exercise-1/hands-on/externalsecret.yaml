# External Secrets Operator – ExternalSecret
# Fetches payment-service-secret from AWS Secrets Manager and creates
# the Kubernetes Secret "payment-secret" in the payment namespace.
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: payment-external-secret
  namespace: payment
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secret-store
    kind: SecretStore
  target:
    name: payment-secret      # Kubernetes Secret to create
    creationPolicy: Owner
  data:
    - secretKey: DB_HOST
      remoteRef:
        key: payment-service-secret
        property: DB_HOST
    - secretKey: DB_USER
      remoteRef:
        key: payment-service-secret
        property: DB_USER
    - secretKey: DB_PASSWORD
      remoteRef:
        key: payment-service-secret
        property: DB_PASSWORD
