apiVersion: v1
kind: Service
metadata:
  name: cpu-load-service
  namespace: exercise22
  labels:
    app: cpu-load-app
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: cpu-load-app
