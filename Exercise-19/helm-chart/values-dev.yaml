replicaCount: 1

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 200m
    memory: 256Mi

configData:
  APP_ENV: "dev"
  APP_MESSAGE: "Hello from the DEV Environment!"

secretData:
  API_KEY: "dev-secret-key-xyz"
  DB_PASSWORD: "dev-db-password-123"

autoscaling:
  enabled: false

pdb:
  enabled: false
